server:
  port: 8080

spring:
  application:
    name: benchmark-agent

agent:
  source-region: turkey
  central-api-url: http://localhost:8082
  targets:
    - region: frankfurt-local-test
      host: localhost
      port: 8081